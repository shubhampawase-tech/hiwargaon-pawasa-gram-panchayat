import { serve } from 'https://deno.land/std@0.224.0/http/server.ts';
serve(async()=>{try{const key=Deno.env.get('OPENWEATHER_API_KEY');if(!key)return new Response(JSON.stringify({error:'Weather key missing'}),{status:500});const lat=Deno.env.get('VILLAGE_LAT')||'19.55';const lon=Deno.env.get('VILLAGE_LON')||'74.20';const r=await fetch(`https://api.openweathermap.org/data/3.0/onecall?lat=${lat}&lon=${lon}&units=metric&appid=${key}`);return new Response(await r.text(),{status:r.status,headers:{'content-type':'application/json','access-control-allow-origin':'*'}})}catch(e){return new Response(JSON.stringify({error:String(e)}),{status:500})}});
