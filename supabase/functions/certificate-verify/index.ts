import { serve } from 'https://deno.land/std@0.224.0/http/server.ts';import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
serve(async(req)=>{const token=new URL(req.url).searchParams.get('token');const sb=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_ANON_KEY')!);const {data,error}=await sb.from('certificates').select('certificate_no,certificate_type,public_payload,issued_at,revoked_at').eq('qr_token',token).single();return new Response(JSON.stringify(error?{valid:false}:{valid:!data.revoked_at,certificate:data}),{status:error?404:200,headers:{'content-type':'application/json','access-control-allow-origin':'*'}})});
